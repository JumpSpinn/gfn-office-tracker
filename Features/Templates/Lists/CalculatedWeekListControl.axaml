<Styles
	xmlns="https://github.com/avaloniaui"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:listcontrols="using:OfficeTracker.Views.Controls.Lists"
	xmlns:controls="using:OfficeTracker.Views.Controls"
	xmlns:models="using:OfficeTracker.Models"
>
	<Design.PreviewWith>
		<listcontrols:CalculatedWeekListControl Width="450" />
	</Design.PreviewWith>

	<Style Selector="listcontrols|CalculatedWeekListControl">
		<!-- Set Defaults -->
		<Setter Property="Template">
			<ControlTemplate>
				<!-- MAIN GRID -->
				<Border
					BorderThickness="1"
					Padding="5"
					Margin="0 10"
					CornerRadius="4"
				>
					<Border.Background>
						<LinearGradientBrush StartPoint="100%,0%" EndPoint="100%,100%" Opacity=".5">
							<GradientStop Color="#10FFFFFF" Offset="0.0"/>
							<GradientStop Color="#05FFFFFF" Offset="1.0"/>
						</LinearGradientBrush>
					</Border.Background>
					<Border.BorderBrush>
						<LinearGradientBrush StartPoint="100%,0%" EndPoint="100%,100%" Opacity=".5">
							<GradientStop Color="#20FFFFFF" Offset="0.0"/>
							<GradientStop Color="#40FFFFFF" Offset="1.0"/>
						</LinearGradientBrush>
					</Border.BorderBrush>

					<Grid
						RowDefinitions="Auto,Auto,Auto"
						HorizontalAlignment="Stretch"
						RowSpacing="5"
					>
						<!-- TITLE BAR -->
						<Grid
							Grid.Row="0"
							ColumnDefinitions="Auto,*,Auto"
						>
							<Label
								Grid.Column="0"
								Content="{TemplateBinding StartWeekDateTime, Converter={StaticResource DateTimeToDateConverter}}"
								FontWeight="Light"
								FontSize="12"
								HorizontalAlignment="Center"
								VerticalAlignment="Center"
							/>
							<Label
								Grid.Column="1"
								Content="{TemplateBinding WeekName}"
								FontWeight="Bold"
								FontSize="14"
								HorizontalAlignment="Center"
								VerticalAlignment="Bottom"
							/>
							<Label
								Grid.Column="2"
								Content="{TemplateBinding EndWeekDateTime, Converter={StaticResource DateTimeToDateConverter}}"
								FontWeight="Light"
								FontSize="12"
								HorizontalAlignment="Center"
								VerticalAlignment="Center"
							/>
						</Grid>

						<!-- STATS -->
						<controls:StatisticControl
							Grid.Row="1"
							AddButtonEnabled="False"
							AddButtonVisible="False"
							HomeOfficeDays="{TemplateBinding HomeOfficeDays}"
							OfficeDays="{TemplateBinding OfficeDays}"
							HomeOfficeQuote="{TemplateBinding HomeOfficeTargetQuoted}"
							OfficeQuote="{TemplateBinding OfficeTargetQuoted}"
						/>

						<!-- DAYS -->
						<ItemsControl
							Grid.Row="2"
							ItemsSource="{TemplateBinding WeekDays}"
							Margin="4 4 4 2"
						>
							<ItemsControl.ItemsPanel>
								<ItemsPanelTemplate>
									<UniformGrid
										Rows="1"
										Columns="5"
										ColumnSpacing="8"
										HorizontalAlignment="Stretch"
									/>
								</ItemsPanelTemplate>
							</ItemsControl.ItemsPanel>
							<ItemsControl.ItemTemplate>
								<DataTemplate DataType="models:WeekDayModel">
									<StackPanel
										Orientation="Vertical"
										Spacing="6"
										ToolTip.Tip="{Binding TypeName}"
										ToolTip.Placement="Top"
										ToolTip.VerticalOffset="0"
									>
										<Border
											HorizontalAlignment="Stretch"
											BorderThickness="1"
											CornerRadius="2"
											Height="30"
										>
											<Border.Background>
												<LinearGradientBrush StartPoint="100%,0%" EndPoint="100%,100%" Opacity="1">
													<GradientStop Color="Transparent" Offset="0.0" />
													<GradientStop Color="#09FFFFFF" Offset="1.0" />
												</LinearGradientBrush>
											</Border.Background>
											<Border.BorderBrush>
												<LinearGradientBrush StartPoint="100%,0%" EndPoint="100%,100%" Opacity="1">
													<GradientStop Color="Transparent" Offset="0.0" />
													<GradientStop Color="#35FFFFFF" Offset="1.0" />
												</LinearGradientBrush>
											</Border.BorderBrush>
											<Label
												Content="{Binding Date, StringFormat='{}{0:ddd}.'}"
												FontWeight="Regular"
												FontSize="12"
												VerticalAlignment="Center"
												HorizontalAlignment="Center"
											/>
										</Border>
										<Border
											HorizontalAlignment="Stretch"
											Height="2"
											BorderThickness="0"
											CornerRadius="2"
											Background="{Binding HexColor}"
										/>
									</StackPanel>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</Grid>
				</Border>
			</ControlTemplate>
		</Setter>
	</Style>
</Styles>
